
PREFIX		=	@HOME@/.local
SHAREDIR	=	@SHAREDIR@

GO111MODULE	=	auto
GOBIN		=	${PREFIX}/bin

all:
	go build -o edpad

install: all
	go env -w GOBIN=${GOBIN}
	go install
	mkdir -p ${SHAREDIR}
	cp -f edpad.css edpad.png edpad.glade ${SHAREDIR}

